public class Main {
    public static void main(String[] args) {

        // read in sigs and phis
        CSVReader csvReader = new CSVReader();

        // build the two arrays with this shit? idk

        Person[] sigs = new Person[0];
        Person[] phis = new Person[0];

        GuestMap guestMap = new GuestMap(sigs, phis);

        // use the guestMap to create a label text document

    }
}
